# 闪念 - 创意随手记应用开发过程

## 项目简介

**闪念**（原名：思维基因）是一款温暖的创意随手记应用，帮助用户捕捉灵感、管理想法、发现创意之间的关联。

### 在线体验地址
👉 https://web-ochre-nu-92.vercel.app

---

## 开发过程

### 第一阶段：项目初始化

使用 Flutter 框架创建跨平台应用：
```bash
flutter create idea_genome
cd idea_genome
flutter run
```

### 第二阶段：核心架构设计

采用 **Clean Architecture** 分层架构：

```
lib/
├── core/           # 核心配置
│   ├── constants/  # 颜色、间距、字体常量
│   ├── theme/      # 应用主题
│   └── utils/      # 工具类
├── data/           # 数据层
│   ├── datasources/ # 本地数据源
│   ├── models/     # 数据模型
│   └── repositories/# 仓库实现
├── domain/         # 领域层
│   ├── entities/   # 实体定义
│   └── repositories/# 仓库接口
├── presentation/    # 表现层
│   ├── blocs/      # 状态管理
│   ├── pages/      # 页面
│   └── widgets/    # 组件
└── services/       # 服务层
```

### 第三阶段：UI设计与实现

#### 1. 温暖治愈色系设计
```dart
// 主色调 - 珊瑚橙 (温暖、活力)
static const Color primary = Color(0xFFFF9B7B);
static const Color primaryLight = Color(0xFFFFBFA0);
static const Color primaryDark = Color(0xFFE87B5C);

// 辅色调 - 天空蓝 (平静、安心)
static const Color secondary = Color(0xFF7EC8E3);
```

#### 2. 底部导航栏
- 首页：记录和浏览想法
- 图谱：可视化创意网络
- 收藏：收藏的想法
- 设置：应用配置

#### 3. 首页组件
- 问候卡片（根据时间段动态问候）
- 快速记录入口
- 灵感提示卡片
- 今日闪念列表

### 第四阶段：核心功能开发

#### 1. 快速记录功能
- 四种类型：想法💡、项目🚀、问题❓、灵感✨
- 支持文字、标签输入
- 友好的底部弹窗交互

#### 2. 图谱功能（核心亮点）
- 基于相似度算法自动关联想法
- 物理引擎驱动的力导向布局
- 支持缩放、拖拽、聚焦操作
- 点击节点查看详情
- 类型分类筛选
- 关键词搜索

**图谱核心算法：**
```dart
double _calculateSimilarity(Idea a, Idea b) {
  double score = 0.15;
  // 类型相同 +0.25
  // 标题相似 +0.15~0.30
  // 标签重叠 +0.35
  // 内容关键词重叠 +0.4
  return score.clamp(0.0, 1.0);
}
```

#### 3. 详情页
- 完整内容展示
- 媒体附件显示（图片、视频、涂鸦）
- 标签显示
- 创建/修改时间

#### 4. 涂鸦板功能
- 自由绘制
- 颜色选择
- 保存为图片
- 跨平台兼容（Web/Mobile）

### 第五阶段：状态管理

使用 **BLoC 模式** 管理状态：

```dart
// IdeaBloc 处理想法的增删改查
// IdeaEvent: CreateIdea, UpdateIdea, DeleteIdea, LoadIdeas
// IdeaState: loaded, loading, error
```

### 第六阶段：平台适配

#### Web 端
- 响应式布局
- 键盘快捷键支持
- 跨平台文件处理

#### Mobile 端
- 触摸交互优化
- 媒体文件本地存储
- 相机/相册集成

---

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Flutter 3.24.0 |
| 状态管理 | flutter_bloc |
| 本地存储 | shared_preferences, path_provider |
| 架构 | Clean Architecture |
| 平台 | Web, Android, iOS, Windows |

---

## 遇到的问题与解决

### 问题1：Android SDK 35 兼容性问题
- **现象**：JDK 21 与 Android SDK 35 的 jlink 冲突
- **解决**：使用 AGP 8.3.0 + Gradle 8.6 组合

### 问题2：Web 平台文件存储
- **现象**：dart:io 在 Web 平台不可用
- **解决**：使用条件导入，实现平台特定处理

### 问题3：图谱性能优化
- **现象**：粒子动画导致移动端卡顿
- **解决**：减少粒子数量，优化渲染逻辑

---

## 部署上线

### Web 版本部署到 Vercel

```bash
# 1. 构建 Web 版本
flutter build web

# 2. 进入构建目录
cd build/web

# 3. 安装 Vercel CLI
npm install -g vercel

# 4. 登录并部署
vercel --yes

# 5. 获得在线链接
# https://web-ochre-nu-92.vercel.app
```

---

## 未来优化方向

1. **数据同步**：实现云端备份和多设备同步
2. **分享功能**：一键生成分享图片/链接
3. **AI 助手**：智能标签推荐、内容建议
4. **社区功能**：想法广场，灵感碰撞
5. **更多媒体支持**：语音记录、视频笔记

---

## 致谢

感谢 Flutter 社区提供的优秀框架和包，让跨平台开发变得简单高效。

---

**在线体验**：👉 https://web-ochre-nu-92.vercel.app

**项目源码**：如需源码，请访问 GitHub 仓库（需要手动配置）

---

*记录每一个闪念，连接无限创意！*
